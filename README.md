# django-page-transitions

A proof of concept repo to show transition abilities for bigger server side rendered django projects.

## Running the Example

After cloning, just exec:

```
$ docker-compose up
```